This is where I explain what the project is and what it does
how to clone it and run it

step 1 takes a csv from my googlesheet data sheet (just patterns from knitting patterns for now)
step 2 opens the file and parses the data
step   maybe creates a dictionary of atributes? or maybe better to create a knitting pattern instance and feed right into it.
step  create pattern class with knitting pattern and sewing pattern children
step  decide what attributes and methods the class needs
step  one of the methods should be to output a list of all the data associated with the pattern
step  some of the fields will be objects in another class (designer and maybe projects need to create separate classes for these, maybe later)
step  create a collection of pattern objects
step  maybe implement some searching or filtering but maybe not at this stage
step  decide on final output form for this stage (text, markdown, html)

